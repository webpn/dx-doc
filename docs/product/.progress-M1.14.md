# M1.14 Progress — Write integrity, audit and publication correctness

**Milestone goal:** A write is atomic, recorded, conflict-safe, and cannot leak content it excluded.

**Delivers:** REQ-FDN-025, REQ-NFR-015 · **completes** REQ-SEC-006, REQ-SEC-012, REQ-AUTH-005, REQ-VER-003, REQ-VER-005, REQ-VER-006, REQ-IMP-005

**Depends on:** M1.13 ✅ complete

---

## Split into commit-sized units (3-6 units)

### Unit 1: Audit log coverage — entity lifecycle events

- **Scope:** Add `appendLog` calls for entity create/modify/delete across all entity types
- **Files to change:** `src/application/tracking/tracking-service.ts`, `src/application/tracking/tracking-service.test.ts`
- **Acceptance:** Every entity create/update/delete method in TrackingService appends an audit log entry; tests prove each event class

### Unit 2: Audit log coverage — publication, login, permission changes, MCP

- **Scope:** Add `appendLog` calls for publication, rollback, export, login/logout, permission changes, MCP calls
- **Files to change:** `src/application/tracking/tracking-service.ts`, `src/api/auth/routes.ts`, `src/api/mcp/server.ts`, and related test files
- **Acceptance:** Tests prove audit entries for each remaining event class in spec §17.4

### Unit 3: Optimistic concurrency everywhere

- **Scope:** Add `expectedUpdatedAt` parameter to all ~15 update methods; return `stale_write` conflict
- **Files to change:** `src/application/tracking/tracking-service.ts`, `src/application/ports/tracking-repositories.ts`, `src/infrastructure/persistence/sqlite-tracking-repositories.ts`, and test files
- **Acceptance:** Two concurrent edits to any mutable entity produce a rejected save with conflict message; tested per entity type

### Unit 4: Publication non-publishable content fix + changelog completeness

- **Scope:** Fix `publishVersion` to exclude non-publishable free pages; extend changelog diff to cover modules, destinations, pages, flows
- **Files to change:** `src/application/tracking/tracking-service.ts`, `src/application/ports/tracking-repositories.ts`, `src/infrastructure/persistence/sqlite-tracking-repositories.ts`, and test files
- **Acceptance:** A free page marked non-publishable is provably absent from a published version; changelog covers all entity types

### Unit 5: Transactional boundaries for multi-row operations

- **Scope:** Wrap `publishVersion`, `setFlowGraph`, batch write endpoint in transactions
- **Files to change:** `src/application/tracking/tracking-service.ts`, `src/api/tracking/routes.ts`, and test files
- **Acceptance:** Batch write failing on item 40 leaves nothing behind; publication either completes fully or not at all; flow-graph replacement cannot leave dangling edges

### Unit 6: Query-path indexes (REQ-NFR-015)

- **Scope:** Add database indexes for all foreign keys and custom_id lookups via migration
- **Files to change:** `db/migrations/` (new migration), `src/infrastructure/persistence/migrations.test.ts`
- **Acceptance:** Reconciliation report and search sync meet REQ-NFR-002/004 targets; drift-guard test passes

---

## Status

| Unit | Description                                      | Status         |
| ---- | ------------------------------------------------ | -------------- |
| 1    | Audit log — entity lifecycle events              | ✅ Done        |
| 2    | Audit log — publication, login, permissions, MCP | ✅ Done        |
| 3    | Optimistic concurrency everywhere                | ✅ Done        |
| 4    | Publication fix + changelog completeness         | ✅ Done        |
| 5    | Transactional boundaries                         | ⬜ Not Started |
| 6    | Query-path indexes                               | ⬜ Not Started |

---

## Notes

- Each unit must pass all four checks independently: `npm run typecheck && npm run lint && npm run format:check && npm test`
- Units are committed individually as conventional commits referencing REQ IDs and M1.14
- Requirement rows updated only at milestone close (step 7 of skill), not per unit
- This file is deleted before finishing the milestone
